<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kevinz.huiju">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".HuijuApplication"
        android:allowBackup="true"
        android:icon="@drawable/huiju"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/DayTheme">
<activity android:name=".MainActivity">
    <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
    </intent-filter>
</activity>
        <activity android:name=".ui.guoke.GuokeDetailsActivity"/>
        <activity android:name=".ui.history.HistoryDetailsActivity"/>
        <activity android:name=".ui.home.CollectionActivity"/>
        <activity android:name=".ui.video.PalyActivity"/>
        <activity android:name=".ui.home.CollectDetailsActivity"/>
        <service android:name=".ui.home.NightMode"/>
    </application>

</manifest>
